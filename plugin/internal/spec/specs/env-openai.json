{
  "id": "env-openai",
  "name": "仅环境变量（OpenAI 兼容）",
  "description": "不修改任何文件，只把指定网关打印成 OPENAI_BASE_URL / OPENAI_API_KEY 的 export / set / powershell 三种形式，适用于任何吃这两个变量的客户端（含 WSL / 容器 / 自写脚本）。模型名由客户端自己的参数决定，网关按请求里的 model 路由。",
  "detect": {
    "env": ["OPENAI_API_KEY"]
  },
  "env": {
    "base_url": "OPENAI_BASE_URL",
    "api_key": "OPENAI_API_KEY",
    "style": "export"
  },
  "restart_note": "环境变量只对之后启动的进程生效；已打开的客户端需重启。",
  "protocols": ["openai-chat"]
}
